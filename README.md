# Object Detection API

A simple FastAPI service for object detection using pre-trained YOLOv8 models.

## 🚀 Setup

1. Clone the repo and install dependencies:
   ```bash
   pip install -r requirements.txt

# Run the API
uvicorn app:app --reload

